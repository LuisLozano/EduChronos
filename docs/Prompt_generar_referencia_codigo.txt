Genera un fichero referencia-codigo-solver.md: un índice de API PURO del
módulo solver/, construido EXCLUSIVAMENTE a partir del código fuente actual.
Si ya existe un fichero con ese nombre, IGNORA su contenido y sobrescríbelo;
no tomes nada de él.

NO describas comportamiento, flujo de ejecución, responsabilidades, ni en qué
fase del proyecto entra cada cosa. NO interpretes el código. Describe solo la
superficie pública/package-private, leída directamente de las firmas.

Recorre todos los ficheros .java de solver/src/main/java y solver/src/test/java.
Ordena por paquete (domain, cpsat, io, cli) y, dentro de cada paquete,
alfabéticamente. Para cada tipo:

- Nombre, visibilidad (public / package-private), y clase de tipo
  (class, record, enum, interface; marca final si lo es).
- Paquete.
- Records: lista de componentes con su tipo.
- Enums: lista de constantes.
- Clases/interfaces: firmas EXACTAS de constructores y métodos (públicos y
  package-private), con tipos de parámetro y de retorno tal cual aparecen en
  el código. No expliques qué hacen.
- "Consume:" una línea por tipo, listando los demás tipos del módulo
  (es.yaroki.educhronos.solver.*) que aparecen en sus firmas o imports.

Encabezado: fecha actual y la salida de `git rev-parse --short HEAD`.

EXCLUYE: sección de flujo principal, sección de "fuera de alcance",
prosa descriptiva, conteos de restricciones, referencias a fases del roadmap.
Si un método tiene Javadoc, ignóralo: solo la firma cuenta.

Guárdalo en docs/referencia-codigo-solver.md. Si ya hay una copia en esa ubicación, crea un backup y elimina el antiguo.
